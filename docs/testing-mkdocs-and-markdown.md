# Let's show you an atx. style Header 1
## Let's show you an atx. style Header 2
### Let's show you an atx. style Header 3
#### Let's show you an atx. style Header 4
##### Let's show you an atx. style Header 5
###### Let's show you an atx. style Header 6
***
Let's show you an Setex style Header 1
===
Let's show you an Setex style Header 2 
---
This is my first paragraph using a markdown language, paragrpahs created by typing on a new line. 

To create a new paragraph just separate it with a blank line

In this example we are having a new three paragraphs
___
> Here is a block quoting example in markdown language.

> Here is a test for creating a new block quote separated by a blank line 

Here is a new paragraph separated a block quote with a blank line, after a paragraph we are creating a new, nested block quote
> Here is usual, not nested block quote. 
>> Here is a nested block quote
> Here again is a regular block quote, not separated by anything.
>
> Okay, to get back to the first level, use a greater then sign again.

Let's use a paragraph element to separate a block quote with an unordered list by using an asterisks, or pluses, or hyphens.
***
* Here is an unordered list using an asterisk
* Here is an unordered list by using an asterisk
* Here is again unordered list by using an asterisk
***
+ Here is an unordered list by using a plus sign
+ Here is again an unordered list bu using a plus sign
+ Here is again an unordered list by using a plus sign
***
- Here is another example of an unordered list by using a hyphen
- Here is another example of an unordered list by using a hyphen 
- Here is another example of an unordered list by using a hyphen 
***
1. Here let's use an ordered list
2. Here let's use an ordered list
3. Here let's use an ordered list
4. Let's use an ordered list again
***
Here is a new paragraph to talk about a code blocks,to create a code block we need to indent a new line by 4 spaces or by tab.
    here is a block code, I am interesting to take a look into it.
    here is a second line of a code block, I am curious as well about it.
    as well as about a third line of a code block.
***
[Python.org](https://www.python.org/)- this is an inline link
[Data Cleaning](docs/data-cleaning.md) - this is a relative path link
[This is a reference link][Data Cleaning]
[Data Cleaning]: http://127.0.0.1:8000/data-cleaning/ "Data Cleaning"
***
Let's test emphasis: *here is an asterisk emphasis* and here is an _underscore emphasis_ and let's talk about other emphasis such **double asterisk emphasis** and __double underscore__ emphasis
***
Okay, single asterisk is about making a text in italick style and the same goes for a single underscore, but for making a text bold, we would use doulbe asterisk or double underscore.
***
Here is an example of a code, and how to make it: wrap it inside a backtick quiotes\`, like in this example will be shown a code `str.strip()`
***
Here is a simple syntax for placing a link use an "!" before a square brakckets and follow the same syntax for inline, reference style, and relative path links.
***
Here let's talk about a backslash escape characters such as backslash, backtick, asterisk, underscore, curly braces, square braces, parentheses, hash mark, plus sign, minus sing(hyphen), dot, and exclamation mark