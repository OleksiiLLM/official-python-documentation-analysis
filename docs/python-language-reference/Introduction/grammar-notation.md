# Grammar Notation

In the context of Python, **Grammar Notation** is the specific "meta-language" or set of symbols used in the official documentation to write down Python's strict grammar rules.

Think of it like the legend on a map. The map itself shows you where the roads go (the Grammar Definition), but the legend tells you that a dashed line means a dirt road and a solid red line means a highway (the Grammar Notation).